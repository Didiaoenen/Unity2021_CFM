local TargetProxyBase = class("TargetProxyBase")

TargetProxyBase.__getter.Target = function (this, k)
    return self.target
end

TargetProxyBase.__getter.DefaultMode = function (this, k)
    return BindingMode.OneWay
end

function TargetProxyBase:ctor(target)
    if not target then
        error("")
        return
    end

    self.target = target
    self.targetName = "target"
end

function TargetProxyBase:IsAlive(target)
    try
    {
        function ()
            
        end,
        catch = function ()
            return false
        end
    }
end

return TargetProxyBase