require("Debug.LuaPanda").start("127.0.0.1", 8818)

require("LuaFramework.Global")

function Main()
    require("Game.TestOOP")

    WindowManager.Open("Game.TestV", {binder = binder})

    Executors.RunOnCoroutineNoReturn(Coroutine.GetEnumerator(function ()
        Coroutine.Wait(15)

        -- WindowManager.Current:Hide()
    end))
end

function __G__TRACKBACK__(msg)
    error("----------------------------------------")
    error("LUA ERROR: " .. tostring(msg) .. "\n")
    error(debug.traceback())
    error("----------------------------------------")
end

local status, msg = xpcall(Main, __G__TRACKBACK__)
if not status then
    print(msg)
end